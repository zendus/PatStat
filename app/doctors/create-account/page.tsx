import { DocCreation } from "@/widgets/Admin/Auth";

export const dynamic = 'force-dynamic';

const DocAccount = () => {
    return(
        <main>
            <DocCreation />
        </main>
    )
};

export default DocAccount;